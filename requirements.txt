# LLM & Agent
langchain>=0.3.0
langchain-ollama>=0.2.0
langchain-openai>=0.2.0
langchain-chroma>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0
ollama

# MCP (Phase 0 实测：streamable-http transport 需 Accept: application/json, text/event-stream)
mcp
langchain-mcp-adapters

# RAG
chromadb>=0.5.0
rank_bm25>=0.2.2
jieba>=0.42.1

# Web Search & Scraping
ddgs
httpx[socks]
beautifulsoup4

# Config / 数据
python-dotenv
PyYAML

# CLI
rich

# Web UI
streamlit>=1.30

# Verifier 跑 pytest 用（hwHandler 自身依赖；workspace 内若需更高版本 sandbox 自己装）
pytest>=8.0

# Evaluation (Step 4，预留)
ragas>=0.1.0
datasets
